{# templates/admin/dashboard.html.twig #}
{# templates/admin/dashboard.html.twig #}
{% extends '@EasyAdmin/layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-4">
                {# <a href="{{ path('Booking') }}" class="card bg-primary text-white text-center p-4 mb-3"> #}
                    <h4>Gestion des Réservations</h4>
                    <i class="fas fa-calendar-alt fa-3x"></i>
                </a>
            </div>
            <div class="col-md-4">
                {# <a href="{{ path('Contact') }}" class="card bg-success text-white text-center p-4 mb-3"> #}
                    <h4>Gestion des Contacts</h4>
                    <i class="fas fa-envelope fa-3x"></i>
                </a>
            </div>
            <div class="col-md-4">
                {# <a href="{{ path('Service') }}" class="card bg-info text-white text-center p-4 mb-3"> #}
                    <h4>Gestion des Services</h4>
                    <i class="fas fa-concierge-bell fa-3x"></i>
                </a>
            </div>
        </div>
    </div>
{% endblock %}