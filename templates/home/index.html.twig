{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    {{ include('/component/navbar.html.twig') }}

    <div class="hero">
        <h1>Auberge Georgio</h1>
        <h2>Restaurant Italien</h2>
    {% if bookingStatus and bookingStatus.canBook %}
    <button type="button" class="" data-bs-toggle="modal" data-bs-target="#reservationModal">
        Réserver
    </button>
{% else %}
    <p>Les réservations sont actuellement désactivées.</p>
{% endif %}
    </div>

    <!-- Modal -->
    

    <div class="info">
        <section>
            <h2>A PROPOS DE NOUS</h2>
            <p>Toute l’équipe de l’Auberge Georgio est heureuse de vous accueillir dans un cadre chaleureux en plein cœur de Vieux Village de Saint-Prix et vous propose une carte aux saveurs authentiques d'Italie.<br> 
            Situé en haut de la ville, vous aurez une vue imprenable sur tout Paris à quelques pas de là.<br> 
            Venez découvrir ce restaurant de caractère et laissez-vous tenter par nos produits choisis avec soin et cuisinés avec passion.</p>
        </section>
        <img src="{{ asset('images/facade.jpg') }}" alt="restaurant italien">
    </div>

    <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            {% for image in images %}
                <div class="carousel-item {% if loop.first %}active{% endif %}">
                    <img src="{{ asset('uploads/galerie/' ~ image.photo) }}" class="d-block" alt="{{ image.description }}">
                </div>
            {% endfor %}
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>
    

    <div class="horaires">
        <h2>HORAIRES:</h2>
        <p>De Mardi au Samedi : 12h00 - 15h00 / 19h00-22h30<br><br>
            Dimanche : 12h00 - 15h00.
        </p>
    </div>


<!-- Modal pour afficher les images en grand -->
<div class="modal fade" id="imageModal" tabindex="-1" aria-labelledby="imageModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-body">
                <div id="modalCarousel" class="carousel slide" data-bs-ride="carousel">
                    <div class="carousel-inner">
                        {% for image in images %}
                            <div class="carousel-item {% if loop.first %}active{% endif %}">
                                <img src="{{ asset('uploads/galerie/' ~ image.photo) }}" class="d-block w-100" alt="{{ image.description }}">
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



<script>
    document.addEventListener('DOMContentLoaded', function () {
        const images = document.querySelectorAll('.carousel-item img');
        const modal = new bootstrap.Modal(document.getElementById('imageModal'), {
            keyboard: true
        });

        images.forEach((img, index) => {
            img.addEventListener('click', function () {
                const modalCarousel = document.querySelector('#modalCarousel .carousel-inner');
                const modalItems = modalCarousel.querySelectorAll('.carousel-item');

                // Active the clicked image in modal
                modalItems.forEach((item, i) => {
                    item.classList.remove('active');
                    if (i === index) {
                        item.classList.add('active');
                    }
                });

                modal.show();
            });
        });

        // Add click event to swap images within the modal
        const modalImages = document.querySelectorAll('#modalCarousel .carousel-item img');
        modalImages.forEach((modalImg, modalIndex) => {
            modalImg.addEventListener('click', function () {
                const nextIndex = (modalIndex + 1) % modalImages.length;
                const modalItems = document.querySelectorAll('#modalCarousel .carousel-item');

                modalItems.forEach((item, i) => {
                    item.classList.remove('active');
                    if (i === nextIndex) {
                        item.classList.add('active');
                    }
                });
            });
        });
    });
</script>
    {{ include('/component/footer.html.twig') }}
{% endblock %}